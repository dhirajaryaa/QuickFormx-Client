import { Input } from '@/components/ui/input'

function SearchBar() {
    return (
        <Input type="text" placeholder="Search Form ..." className={"bg-muted max-w-xs w-auto sm:w-xs "} />
    )
}

export default SearchBar
